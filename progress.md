Original prompt: Implement Vergil as a playable graph unit with a tactical HUD, explicit Command Mode, right-click movement, node attachment with rail travel, follow camera, slimmer controls, and more vibrant graph travel glow.

- 2026-03-18: Re-anchored in the correct galaxy-skybox worktree and confirmed the existing overnight Vergil/retrieval changes before continuing the playable-unit pass.
- 2026-03-18: Tactical HUD pass landed in src/core/orb.js and src/styles/orb.css. Added CMD/FOLLOW/ROTATE buttons, ASK submit, and a Vergil state chip while preserving candidate, armed, clarification, and error flows.
- TODO: Wire HUD control callbacks into src/main.js, remove the old CTL/rotate-speed/zoom strip, and make the HUD the source of truth for command/follow/rotate state.
- TODO: Add shortest-path and edge-pair helpers in src/core/tesseract.js, then connect graph-scene travel overlay to Vergil rail movement.
- TODO: Extend src/core/vergil.js with explicit movement states and expose the hooks main.js needs for follow camera and rail traversal.
- TODO: Run build/tests plus a quiet Playwright visual loop after each meaningful UI/scene change.
- 2026-03-18: Integrated the tactical HUD into main.js, removed the old CTL/zoom/rotate strip, wired CMD/FOLLOW/ROTATE, added right-click command issuance, follow-camera motion, and connected rail travel to the graph overlay.
- 2026-03-18: Verified the playable-unit slice with `npm run build`, `npm run test`, and quiet headless captures for ambient HUD, active controls, free move, attachment, and live rail travel.
- Note: the HUD state chip can miss a frame in very fast headless travel captures even though attach and rail-travel behavior itself is visible and working.
- 2026-03-18: Tightened the command-mode feel. Main.js now keeps a custom command reticle, live hover targeting, cached spam-click command points, a smoother follow-camera path, and a world-command plane anchored to Vergil before the selected node.
- 2026-03-18: Vergil now stays in `FREE MOVE` after reaching a world point instead of falling back to ambient, and the command marker gained a stronger RTS-style pulse with preview support.
- 2026-03-18: Graph-scene boldness pass landed. Nodes read slightly larger and brighter, and edges read more present without turning the UI chrome into noise.
- 2026-03-18: Verification evidence for this pass is in `output/playwright/vergil-command-reticle-pass-1.png`, `output/playwright/vergil-command-click-pass-1.png`, and `output/playwright/vergil-follow-command-pass-1.png`.
- 2026-03-18: Command responsiveness pass landed. `src/core/vergil.js` now uses graph-scale command bounds, higher active movement speeds, lower free-move drag, and velocity reset on world retarget so spam-clicks behave more like RTS orders than eased cinematic travel.
- 2026-03-18: `src/main.js` now resolves world orders against a deep command sphere around the current camera anchor instead of a shallow foreground depth, which makes distant-space right-clicks land much farther into the visible galaxy.
- 2026-03-18: Scale pass landed. `src/core/tesseract.js` now lays the graph into a larger world volume, `src/main.js` starts farther back and flies wider around nodes, and `src/core/graph-scene.js` brightens nodes and links to hold readability at the new scale.
- 2026-03-18: UI polish already integrated in this slice includes the sleek floating speech text and a monochrome videogame-style Vergil profile card in the tactical HUD.
- 2026-03-18: Verification for the scale/command pass: `npm run build`, `npm run test`, a skill-loop burst in `output/web-game/vergil-command-burst/shot-0.png`, and full-page headless captures in `output/playwright/vergil-command-scale-pass-2.png` and `output/playwright/vergil-follow-scale-pass-2.png`.
- 2026-03-18: Slowed the selected-constellation pulse in `src/core/graph-scene.js` so the selected lane hits on a longer cycle with a heavier peak and slower neighbor echo. Re-verified with `node --check src/core/graph-scene.js`, `npm run build`, `npm run test`, and a fresh headless capture in `output/web-game/pulse-check-slower-1/shot-0.png`.
- 2026-03-18: Rebuilt the persistent Vergil HUD into a true single-row tactical bar. `src/core/orb.js` now folds identity into the left rail and telemetry into the right rail instead of stacking the old header and dossier above the input. `src/styles/orb.css` now targets the short-bar footprint with a compact portrait, slimmer controls, and side-distributed readouts. Verified in `output/playwright/vergil-hud-compact-pass-2.png`.
- 2026-03-18: Tightened empty-space commanding and Vergil movement. `src/main.js` now resolves world commands against a local camera-facing tactical plane around the current focus instead of a far sphere, clamps the playable command volume, and advances rail overlay segments on a tighter threshold. `src/core/vergil.js` now uses a smaller command radius and slower ship-like free-move and rail speeds with less aggressive pursuit boosting. Verified in `output/playwright/vergil-command-clamped-pass-2.png`.
- 2026-03-18: Re-shaped the selected constellation pulse to stay lit longer with a rebound crest, and added a very low-amplitude ambient cousin of that rhythm to the idle edge field so the graph keeps some life at rest without going back to a uniform brightness wash. Re-verified with `node --check src/core/graph-scene.js`, `npm run build`, and `npm run test`.
- 2026-03-18: Left-click node selection now starts a Vergil-driven selection journey instead of immediately blooming. `src/main.js` now routes graph clicks through `requestGraphSelection()`, starts camera easing toward the destination during the ride, waits for Vergil's real rail-arrival callback from `src/core/vergil.js`, then commits the selection and delay-gates the final selection pulse until the arrival wave settles.
- 2026-03-18: Vergil scale increased in `src/core/vergil.js` so he reads as an in-world character instead of a speck, and the rail-arrival callback is now emitted from Vergil’s exact final attachment point.
- 2026-03-18: `src/core/graph-scene.js` now has a softer neutral palette, slightly more inviting idle breathing in both edges and nodes, lower peak selected brightness, a sparse set of naturally hotter baseline links for depth, and a connected-edge arrival wave that originates from the selected node and runs along the actual edge geometry instead of a separate nearby effect.
- 2026-03-18: Verification for this pass: `node --check src/main.js`, `node --check src/core/vergil.js`, `node --check src/core/graph-scene.js`, `npm run build`, and `npm run test` all passed. Neutral-state visual proof is in `output/playwright/vergil-neutral-inviting-pass-1.png`. Headless graph-click targeting is still unreliable for exact ride-path screenshots, so left-click ceremony visuals still need one real-browser proof pass.
- 2026-03-18: Follow-up tuning pass: slowed rail travel in `src/core/vergil.js`, slowed and softened the arrival bloom in `src/main.js`, and reduced the global idle/travel-wide breathing intensity in `src/core/graph-scene.js` so the graph no longer reads like the entire field is flashing during Vergil transit.
- 2026-03-18: Verification for the slower pass: `node --check src/core/vergil.js`, `node --check src/main.js`, `node --check src/core/graph-scene.js`, `npm run build`, `npm run test`, and a quiet neutral capture at `output/playwright/vergil-neutral-slower-pass-2.png`.
- 2026-03-19: Hive Viz visual overhaul merged in the correct `galaxy-skybox` worktree. `src/core/graph-scene.js` now carries the unified idle/selected field, toned selection brightness, hot-link depth variance, camera-anchored skybox layer, distant planets, and translucent nebula veil. `src/core/vergil.js`, `src/core/orb.js`, and `src/styles/orb.css` now carry the slimmer warm-metal Vergil/HUD pass. `src/core/tesseract.js` and `src/main.js` now carry seeded reshuffle, tighter zoom bounds, skybox updates, and camera timing around selection journeys.
- 2026-03-19: Post-restart verification loop rerun with a single headless Playwright session. `npm run build` and `npm run test` both passed again after the merge.
- 2026-03-19: Motion proof artifacts for the overhaul pass:
  - Idle baseline: `.playwright-cli/page-2026-03-19T02-16-45-451Z.png`
  - Selection journey video: `output/playwright/overhaul-pass-1/selection-journey.webm`
  - Selection contact sheet: `output/playwright/overhaul-pass-1/selection-contact.png`
  - Selection keyframes: `output/playwright/overhaul-pass-1/search-selection-01.png`, `output/playwright/overhaul-pass-1/search-selection-02.png`, `output/playwright/overhaul-pass-1/search-selection-03.png`
  - Skybox pan video: `output/playwright/overhaul-pass-1/skybox-pan.webm`
  - Skybox pan contact: `output/playwright/overhaul-pass-1/skybox-pan-contact.png`
  - Shuffle settle video: `output/playwright/overhaul-pass-1/shuffle-settle.webm`
  - Shuffle settle frame: `output/playwright/overhaul-pass-1/shuffle-settle.png`
  - Zoom bound frame: `output/playwright/overhaul-pass-1/zoom-limit.png`
- 2026-03-19: Practical note for future verification: headless direct graph clicks are brittle on the canvas. Sidebar search plus `Enter` triggers the same `onSelect()` path and produces a more repeatable proof of the camera/selection journey without guessing node hit coordinates.
- TODO: If the next polish pass needs tighter cinematic proof, expose a lightweight debug hook for current node screen positions or a deterministic `window.render_game_to_text`/`window.advanceTime` pair so the web-game skill can drive graph selection without relying on approximate canvas coordinates.
- 2026-03-19: Merged the visual-overhaul wave. `src/core/graph-scene.js` now unifies idle and selected field language, tones down white clipping, adds deterministic hot-link/depth variance, camera-anchored far stars, distant planets, and a translucent outer veil. `src/core/vergil.js` now uses the warmer metallic lantern direction, while `src/core/orb.js` and `src/styles/orb.css` collapse the HUD into a slimmer control bar with `SHUFFLE` and no dossier stack. `src/core/tesseract.js` now supports seeded reshuffles, and `src/main.js` now integrates shuffle settle animation, tighter zoom bounds, selection-journey camera timing, and the skybox update hook.
- 2026-03-19: Re-verified after the restart with `npm run build` and `npm run test` (`13/13`). Fresh visual artifacts are in `output/playwright/overhaul-pass-1/`. The most useful proof pieces are `selection-contact.png`, `search-selection-01.png` through `search-selection-03.png`, `shuffle-settle.png`, and `zoom-limit.png`. Motion clips are in `.playwright-cli/video-2026-03-19T02-21-33-481Z.webm` (search-driven selection ride), `.playwright-cli/video-2026-03-19T02-25-02-793Z.webm` (pan/drag skybox), and `.playwright-cli/video-2026-03-19T02-25-39-659Z.webm` (shuffle settle).
- 2026-03-19: Headless graph-click targeting is still less reliable than search-driven selection for proof capture, so the cleanest automated selection demo currently uses sidebar search + Enter to trigger the same journey. If a later pass needs exact in-world click proof, expose a tiny debug hook for current focus/selection or run one headed capture pass with manual coordinates.
- 2026-03-19: While resuming the live loop, a real regression showed up in the zoom proof: the camera could still back out far enough to collapse the graph into a tiny cluster. Fixed in `src/main.js` by tightening `syncCameraBounds()` to clamp the max distance into a much narrower cinematic range instead of scaling almost 1:1 with the graph radius. Re-verified with `npm run build` and `npm run test`; updated before/after captures are in `output/playwright/overhaul-pass-1/zoom-before-fixed.png`, `zoom-after-fixed.png`, `zoom-before-final.png`, and `zoom-after-final.png`.
- 2026-03-19: Follow-up navigation pass landed. `src/main.js` now widens zoom slightly again without reopening the old god-view, passes a real camera-footprint polygon into the minimap, and tracks rail-travel progress segment-by-segment so Vergil’s current edge no longer front-loads as fully lit.
- 2026-03-19: `src/core/minimap.js` now draws the current camera footprint as a directional polygon when available instead of only a generic circular lens, which makes the bottom-left navigator map back to the main 3D view more faithfully.
- 2026-03-19: `src/core/graph-scene.js` now supports a moving beam head (`beamEdgeIndex`, `beamOriginNodeId`, `beamProgress`, `beamStrength`) separate from the persistent lit-edge trail. The current rail segment lights progressively behind Vergil, and completed segments are kept in the recent/lit set instead of the whole active edge igniting at once.
- 2026-03-19: Verification for this pass: `node --check src/main.js`, `node --check src/core/minimap.js`, `node --check src/core/graph-scene.js`, `npm run build`, and `npm run test` all passed. Fresh artifacts are in `output/playwright/planetarium-pass-6/`:
  - `idle-minimap-footprint.png`
  - `selection-arrival-after-search.png`
  - `selection-journey.webm`
  - `selection-journey-contact.png`
  - `selection-journey-contact-early.png`
- TODO: The new minimap footprint is more truthful than the old lens, but if it still feels off in real use the next upgrade is to compute and draw the full world-plane frustum with directional handles instead of a simple polygon/bounds blend.
- TODO: The rail-travel clip needs one tighter manual or deterministic proof pass focused on the mid-journey frames. The overlay logic now uses a progressive beam + persistent trail, but the current automated contact sheet still samples too sparsely to be a perfect visual proof of the effect.
- 2026-03-19: Planetarium/navigation continuation pass landed. `src/main.js` now uses a slightly wider zoom cap (`GRAPH_ZOOM_MAX_FACTOR` and `GRAPH_ZOOM_MAX_DISTANCE_MAX`) while still staying well inside the old god-view range.
- 2026-03-19: Neutral graph-liveliness tuning landed in `src/core/graph-scene.js`. The idle field now keeps a dimmer base while using stronger deterministic hot links, cluster breathing, node sparkle, and hue-preserving glow instead of a flat bright baseline. Selected peaks were kept capped lower so they retain more of their source color instead of clipping hot-white.
- 2026-03-19: Practical verification note: the earlier “stop construction button gets intercepted by canvas” issue did not reproduce once the Playwright loop was pinned to a single named browser session. Real start/stop button clicks both succeeded in-session against the live HUD.
- 2026-03-19: Fresh proof set for this pass is in `output/playwright/planetarium-pass-4/`:
  - `idle.png`
  - `idle-after-tuning.png`
  - `layout-menu-open.png`
  - `construction-start.png`
  - `construction-stop-menu.png`
  - `construction-stopped.png`
  - `psych-profile/selection-journey.webm`
  - `psych-profile/selection-contact.png`
  - `psych-profile/start.png`
  - `psych-profile/mid.png`
  - `psych-profile/arrival.png`
- 2026-03-19: Deterministic sidebar-search journey proof now works with a known graph title (`psychological profile`). The contact sheet in `output/playwright/planetarium-pass-4/psych-profile/selection-contact.png` shows ambient idle, rail-travel, and final lock/attach states in one capture set.
- 2026-03-19: Construction-mode control loop debugged and tightened. The remaining “stop construction” pointer failure was not a canvas-layering bug in the product; it came from the layout menu staying open on `Start construction`, so reopening the menu in verification actually closed it before clicking the hidden stop action. `src/core/orb.js` now closes the layout popover on start, which produces the cleaner UX and makes the real pointer loop deterministic: open layout -> start -> reopen -> stop. Re-verified with sequential headless pointer events plus `npm run build` and `npm run test`.
- 2026-03-19: Neutral field tuning pass landed in `src/core/graph-scene.js`. Folder colors now keep more saturation/lightness, idle edges carry a stronger low-amplitude breathing/shimmer mix, always-hot links stay visible longer, and nodes are slightly larger/more present without pushing selection back into white clipping. Fresh idle proof is in `output/playwright/planetarium-pass-5/shot-0.png`.
- 2026-03-19: Zoom headroom widened slightly again in `src/main.js` so the camera can pull back farther than the post-fix clamp, but still not into the old unusable god-view. Verified with `node --check src/main.js`, `npm run build`, and `npm run test`.
- TODO: The planetarium field is in a better range now, but it still wants another art pass toward “dim base, alive shimmer” from more than one camera angle. Next proof should include both idle wide and selected-neighborhood frames side-by-side so edge/base tuning can be judged against the selection state, not just a single idle crop.
