# Feedback Memory: FPV Paradigm

This memory is superseded by the functional-tool direction.

Historical note:

- Earlier direction emphasized first-person astronaut navigation inside the galaxy
- Current direction prioritizes functional galaxy explorer behavior first

If FPV ideas return, they must support utility instead of replacing it.
