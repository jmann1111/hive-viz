# Feedback Memory: Color Identity

Folder color identity is structural, not decorative.

Rules:

- Use folder-based color regions through the folder color map
- Selection should amplify a node's base color, not flatten it to cyan
- UI chrome should stay monochrome
- Edges should blend their source and target colors

The galaxy owns the color. The interface gets out of the way.
