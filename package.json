{
  "name": "hive-viz",
  "version": "0.1.0",
  "description": "Temporal 3D visualization of The Hive knowledge vault",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "node --watch src/server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "extract": "node scripts/extract.js",
    "eval:orb": "node scripts/eval-orb.js --providers docs/evals/fixtures/providers.sample.json",
    "server": "node src/server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "three": "^0.170.0",
    "d3-force-3d": "^3.0.5",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
