{
  "name": "the-hive",
  "version": "0.1.0",
  "description": "The Hive knowledge explorer and vault visualizer",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "node --watch src/server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "extract": "node scripts/extract.js",
    "eval:retriever": "node scripts/eval-retriever.js --providers docs/evals/fixtures/providers.sample.json",
    "server": "node src/server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "three": "^0.170.0",
    "d3-force-3d": "^3.0.5",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
